# class Vec
    # times: (s) ->
    # plus:  (o) ->
    # minus: (o) ->
    # dot:   (o) ->

# a = new Vec
# b = new Vec
# c = new Vec

# ▸vec(a, b, c, d, e)

    # a = 1 * b * (c + a) * (2 * d)
    
▸vec(a, b)
  
    # r2 = 2 * a
    a * ((b * f) * 1.1 * 2.2 * (2*a)) * b
    # r3 = a * 2
    # r4 = a * 2
    # r5 = a * 2
    # r4 = a + b + a
    # r4 = a - b - a
    
# r = d.dot(e) * f