// koffee 1.16.0
var a, b, r;

a.dot(b);
a.times(Math.cos(b.dot(c)));
a.times(Math.cos(3));
a.times(2);
a.dot(a);
c.times(a.dot(b));
c.times(a.dot(b));
a.times(b.dot(c));
b.dot(b.times(b.dot(b)));
b.dot(b.times(b.dot(b)));
b.dot(b.times(b.dot(b)));
b.dot(b.times(b.dot(b)));
(b.dot(b)) * (b.dot(b));
a.dot(a.times(f));
b.dot(b.times(f).times(f));
b.dot(a.times((b.times(f)).times(1.1).times(2.2).dot(a.times(2))));
b.dot(a.times((b.times(f)).times(1.1 * 2.2).dot(a.times(2))));
b.times(b.dot(a) * ((b.times(f)).times(1.1 * 2.2).dot(a.times(2))));
a.times(b.dot(a.times((b.times(f)).times(1.1 * 2.2).dot(a.times(2)))));
b.times(b.dot(a) * ((b.times(f)).times(1.1).times(2.2).dot(a.times(2))));
a.plus(b).plus(c);
a.minus(b);
a.minus(b).minus(c);
(a.plus(b)).minus(a).plus(b).minus(a);
a.minus(b.minus(a)).minus(b).minus(a);
a.dot(a.plus(b));
c.dot(a.minus(b));;

a.plus(b).minus(c.minus(b).minus(a));
(b.minus(a)).dot(a.minus(b));
b.times(a.length());
(b.minus(a)).to(a.minus(b));
console.log(a.minus(b));
console.log(a.dot(b));
console.log(a.dot(b), b.minus(c), a.dot(d));
console.log(1, b.minus(c), a.dot(d), f);
if (true) {
    r = a.minus(b.dot(c));
    r = a.dot(b);
    r -= a.dot(c);
    r += a.dot(d);
    r *= a.dot(e);
    if (r != null) {
        r;
    } else {
        r = a.times(f);
    }
};

a = 1;

b = c * d;
